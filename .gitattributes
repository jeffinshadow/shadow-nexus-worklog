# Scripts shell precisam de LF sempre (deploy roda em Linux).
*.sh text eol=lf

# Schema SQL: LF para evitar problemas de fim de linha no startup.
*.sql text eol=lf

# Fontes binarias nao devem sofrer normalizacao de fim de linha.
*.woff2 binary
